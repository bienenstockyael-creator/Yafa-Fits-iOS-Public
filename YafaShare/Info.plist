<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Two-tier extraction. GetProduct.js reads the product
			     straight from the live page (past any bot wall the
			     server would hit), so shops like Jacquemus save
			     instantly with no client backfill. On pages where
			     Safari can't deliver the script's results (anti-bot
			     shops that stall injection — Farfetch, Valentino,
			     Cult Gaia via Lens), the controller falls back to the
			     plain URL after one retry and the server stubs the
			     save for the WKWebView backfill. The fallback is why
			     re-enabling the JS is safe: a failed delivery used to
			     hard-fail as "No link to save"; now it degrades. -->
			<key>NSExtensionJavaScriptPreprocessingFile</key>
			<string>GetProduct</string>
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsWebPageWithMaxCount</key>
				<integer>1</integer>
			</dict>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
