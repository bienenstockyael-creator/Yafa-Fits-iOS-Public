<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- URL-only shares, NO JavaScript preprocessing. The
			     GetProduct.js scraping route depended on Safari
			     injecting the script into the shared page — anti-bot
			     shops (Farfetch, Valentino, Cult Gaia) block or stall
			     that injection, Safari then can't deliver the promised
			     results plist, and the save hard-fails with "No link
			     to save". A plain URL share works on every page in
			     every load state; product metadata comes from the
			     server-side scrape in `share-save` instead. -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
			</dict>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
