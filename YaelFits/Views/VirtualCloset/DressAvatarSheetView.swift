// DressAvatarSheetView was the original "open a sheet, run nano-banana,
// show result" flow for dressing the avatar. It was replaced by an inline
// flow in VirtualClosetView so the user can iteratively re-dress without
// leaving the closet. File kept (empty) so the Xcode project reference
// stays valid; safe to delete the file + pbxproj reference together later.
